# DC Digital Twin API

Python package for the Mini DC Digital Twin backend services.

The package contains the FastAPI app, simulator, MQTT ingest worker, alerting worker, and predictive-maintenance scoring worker. See `../../docs/local-stack.md` for local stack and deployment instructions.
